#ifndef CONFIG_H_ /* Include guard */
#define CONFIG_H_

#define MAX_LINE_LEN 81 /* length including `\n` at the end */

typedef struct Result {
    int len;
    int has_errors;
} Result;


#endif /* CONFIG_H_ */
