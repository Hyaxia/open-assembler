Input - from command line, list of parameters representing file names

1. go over the file and replace macros with their internal values


Notice:

- ATM, files with `.asm` are the files we pass to the assembler
- 

